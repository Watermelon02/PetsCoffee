<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.petscoffee">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetsCoffee">
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.petscoffee.activities.PictureActivity"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <activity
            android:name=".activities.WorkActivity"
            android:exported="false" />

        <service
            android:name=".service.WorkService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".activities.PetsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ShopActivity"
            android:exported="false" />
        <activity
            android:name=".activities.BagActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CoffeeShopActivity"
            android:exported="false"
            android:launchMode="singleTask" />
        <activity android:name=".activities.PictureActivity"
            android:exported="false"/>
        <activity
            android:name=".activities.FirstActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>